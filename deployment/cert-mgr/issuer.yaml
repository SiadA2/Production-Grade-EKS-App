apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    # server: "https://acme-v02.api.letsencrypt.org/directory"
    server: "https://acme-staging-v02.api.letsencrypt.org/directory"
    email: "siadadam2@gmail.com"
    privateKeySecretRef:
      name: issuer
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z02432831FEC57GRQFGHB
          region: eu-west-2
      selector:
        dnsZones:
        - "lab.nginxsiad.com"
    - http01:
        ingress:
          class: nginx