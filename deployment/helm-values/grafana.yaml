datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-server.prometheus.svc.cluster.local #This is Prometheus ClusterIP DNS Address, Grafana will query Prometheus over HTTP to get it's metric
        isDefault: true