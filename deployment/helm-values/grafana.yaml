datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: https://prometheus.lab.nginxsiad.com #This is Prometheus ClusterIP DNS Address, Grafana will query Prometheus over HTTP to get it's metric
        isDefault: true